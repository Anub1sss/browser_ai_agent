# Task Hunter

Автономный CLI-инструмент для автоматизации веб-задач с использованием LLM. Task Hunter позволяет выполнять сложные задачи в браузере через естественный язык, используя различные модели искусственного интеллекта.

## 🚀 Основные возможности

- **Автономное выполнение задач** — агент самостоятельно анализирует страницы и выполняет действия
- **Множественные LLM провайдеры** — поддержка custom LLM, OpenAI (GPT-4o, GPT-4o-mini) и GLM (GLM-4.6V-Flash)
- **Профили запуска** — сохранение настроек для разных сценариев использования
- **Очередь задач** — пакетная обработка с приоритетами, ретраями и управлением паузой
- **Постоянные сессии** — сохранение логинов, куки и состояния браузера между запусками
- **Резервные модели** — автоматическое переключение на запасной LLM при ошибках
- **Под-агенты** — планирование сложных задач перед выполнением
- **Безопасность** — запрос подтверждения перед деструктивными действиями
- **Адаптивная обработка ошибок** — автоматическое восстановление после неудачных действий

## 📋 Требования

- Python 3.10 или выше
- Chrome/Chromium браузер
- API ключи для выбранных LLM провайдеров (если не используете custom LLM)

## 🔧 Установка

1. **Клонируйте репозиторий:**
   ```bash
   git clone https://github.com/Anub1sss/browser_ai_agent.git
   cd browser_ai_agent
   ```

2. **Создайте виртуальное окружение:**
   ```bash
   python3 -m venv .venv
   source .venv/bin/activate  # На macOS/Linux
   # или
   .venv\Scripts\activate  # На Windows
   ```

3. **Установите зависимости:**
   ```bash
   pip install -r requirements.txt
   ```

4. **Настройте переменные окружения:**
   ```bash
   cp .env.example .env
   # Отредактируйте .env и добавьте ваши API ключи
   ```

## ⚙️ Настройка

### Переменные окружения

Создайте файл `.env` на основе `.env.example` и укажите необходимые ключи:

- **OpenAI:** `OPENAI_API_KEY=your_key_here` (для моделей GPT-4o, GPT-4o-mini)
- **GLM:** `ZAI_API_KEY=your_key_here` (для модели GLM-4.6V-Flash)

### Первый запуск

При первом запуске Task Hunter автоматически создаст профиль по умолчанию:

```bash
python3 task_hunter.py
```

## 🎯 Быстрый старт

```bash
# Активируйте виртуальное окружение
source .venv/bin/activate

# Запустите Task Hunter
python3 task_hunter.py
```

Выберите опцию `Run single task` и введите вашу задачу на естественном языке, например:

```
Найди топ-3 популярных блюда в Москве, используй рандомайзер для выбора одного, 
найди цены в Яндекс Лавке и создай таблицу в онлайн Excel
```

## 📖 Использование

### Главное меню

При запуске `task_hunter.py` доступны три опции:

1. **Run single task** — выполнить одну задачу
   - Можно изменить параметры запуска перед выполнением
   - Подходит для разовых задач и тестирования

2. **Batch queue** — управление очередью задач
   - Добавление задач с приоритетами
   - Автоматические ретраи при ошибках
   - Пауза и возобновление обработки очереди

3. **Manage profiles** — управление профилями
   - Создание и редактирование профилей запуска
   - Установка профиля по умолчанию
   - Удаление неиспользуемых профилей

### Параметры запуска

Каждый профиль и задача могут иметь следующие параметры:

| Параметр | Описание | Значения по умолчанию |
|----------|----------|----------------------|
| **Provider** | Провайдер LLM | `custom` (browser-use), `openai`, `glm` |
| **Model** | Конкретная модель провайдера | Зависит от провайдера |
| **Temperature** | Температура генерации | Зависит от модели |
| **Persistent session** | Сохранять профиль браузера | `false` |
| **Profile directory** | Директория профиля браузера | `.browser_profile` |
| **Headless** | Запуск без GUI | `false` |
| **Wait between actions** | Задержка между действиями (сек) | `2.0` |
| **Start delay** | Задержка перед началом (сек) | `8` |
| **Fallback** | Использовать запасной LLM | `false` |
| **Sub-agent** | Планирование перед выполнением | `false` |
| **Proxy** | Настройки прокси-сервера | Не используется |

### Поддерживаемые LLM провайдеры

#### Custom LLM (browser-use)
- **Модель по умолчанию:** `bu-latest`
- **Особенности:** Оптимизирована для веб-автоматизации
- **Требования:** API ключ browser-use (если требуется)

#### OpenAI
- **Модели:** `gpt-4o`, `gpt-4o-mini`, `gpt-4-turbo`
- **Требования:** `OPENAI_API_KEY` в `.env`
- **Особенности:** Высокое качество, поддержка vision (скриншоты)

#### GLM (Zhipu AI)
- **Модели:** `GLM-4.6V-Flash`
- **Требования:** `ZAI_API_KEY` в `.env`
- **Особенности:** Быстрая обработка, поддержка vision

## 💡 Примеры задач

### Простые задачи
- "Найди топ-3 популярных блюда в Москве и сохрани их в заметку"
- "Сравни цены на ноутбуки в трех магазинах"
- "Зарегистрируйся на сайте и подтверди email"

### Сложные задачи
- "Найди топ-3 популярных блюда в Москве, используй рандомайзер для выбора одного, найди все цены в Яндекс Лавке, создай таблицу в онлайн Excel с ценами и средним значением, добавь блюдо в корзину с адресом доставки 'Арбат 34', затем удали из корзины"
- "Исследуй рынок недвижимости в Москве, собери данные о ценах на квартиры в 5 районах, создай сравнительную таблицу"
- "Найди информацию о последних новостях в сфере AI, отфильтруй по релевантности и сохрани топ-10 в документ"

### Работа с формами
- "Заполни форму обратной связи на сайте с указанными данными"
- "Найди и заполни все обязательные поля в регистрационной форме"

### E-commerce задачи
- "Добавь товар в корзину, перейди к оформлению, но не завершай заказ"
- "Сравни цены на конкретный товар в разных интернет-магазинах"

## 🗂️ Структура данных

Профили и очередь задач сохраняются в `~/.task_hunter/`:

- **`profiles.json`** — все созданные профили с настройками
- **`queue.json`** — очередь задач с приоритетами и статусами

### Формат профиля

```json
{
  "default_profile": "default",
  "profiles": {
    "default": {
      "provider": "custom",
      "model": null,
      "persistent_session": false,
      "headless": false,
      "wait_between_actions": 2.0,
      "start_delay_seconds": 8,
      "proxy": {
        "server": null,
        "bypass": null,
        "username": null,
        "password": null
      }
    }
  }
}
```

## 🔍 Особенности работы

### Адаптивная обработка ошибок
Агент автоматически адаптируется при неудачных действиях:
- Повторяет действия с вариациями
- Использует альтернативные стратегии
- Запрашивает подтверждение перед деструктивными действиями

### Безопасность
- Запрос подтверждения перед удалением данных из корзины
- Запрос подтверждения перед очисткой форм
- Возможность ручного вмешательства через `wait_for_user`

### Оптимизация производительности
- Настраиваемые задержки между действиями
- Задержка перед началом работы (для загрузки страниц)
- Минимизация лишних скроллов и действий

## 🛠️ Устранение неполадок

### Проблема: Агент не может найти элементы на странице
**Решение:** Увеличьте `wait_between_actions` в профиле, чтобы дать странице больше времени на загрузку.

### Проблема: Ошибки API (502, timeout)
**Решение:** 
- Проверьте правильность API ключей в `.env`
- Для GLM увеличьте таймауты в коде (если доступно)
- Используйте fallback на другой провайдер

### Проблема: Браузер не запускается
**Решение:**
- Убедитесь, что Chrome/Chromium установлен
- Проверьте права доступа к директории профиля
- Попробуйте запустить с `headless: false` для отладки

### Проблема: Задачи выполняются слишком быстро/медленно
**Решение:** Настройте `wait_between_actions` и `start_delay_seconds` в профиле под ваши нужды.

## 📝 Логирование

Логи сохраняются в файлы (если настроено в `.env`):
- `debug.log` — детальные логи для отладки
- `info.log` — информационные сообщения

Уровень логирования настраивается через `BROWSER_USE_LOGGING_LEVEL` в `.env`.

## 🤝 Вклад в проект

Приветствуются любые улучшения! Создавайте issues и pull requests.

## 📄 Лицензия

См. файл LICENSE в репозитории.

## 🔗 Полезные ссылки

- [Документация browser-ai](https://github.com/Anub1sss/browser_ai_agent)
- [OpenAI API](https://platform.openai.com/docs)
- [GLM API](https://open.bigmodel.cn/)

---

**Примечание:** Task Hunter использует AI для автоматизации браузера. Убедитесь, что вы используете его ответственно и в соответствии с правилами использования веб-сайтов.
